/*
 * Stedi Healthcare
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 2024-04-01
 * Contact: healthcare@stedi.com
 * Generated by: https://openapi-generator.tech
 */

use crate::healthcare::models;
use serde::{Deserialize, Serialize};

/// EligibilityCheckRequestProvider : Information about the entity requesting the eligibility check. This may be an individual practitioner, a medical group, a hospital, or another type of healthcare provider.  - You must provide the provider's name as either a person (with `name.person`) or an organization (with `name.organization`).  - You must also provide an identifier - this is typically the provider's [National Provider Identifier](https://www.stedi.com/docs/healthcare/national-provider-identifier) (`npi`). If the provider doesn't have an NPI, you can supply an alternative, such as their `tin` or `ssn`.  - Don't include additional properties, such as `tin` or `address`, unless they are specifically required or suggested by the payer.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct EligibilityCheckRequestProvider {
    /// Additional identification numbers for the provider. Only include when specifically required by a payer.
    #[serde(rename = "additionalInformation", skip_serializing_if = "Option::is_none")]
    pub additional_information: Option<Box<models::EligibilityCheckRequestProviderAdditionalInformation>>,
    /// The provider's address. Only include when specifically instructed by a payer, such as when the provider has multiple locations and you need to identify the specific location making the request.
    #[serde(rename = "address", skip_serializing_if = "Option::is_none")]
    pub address: Option<Box<models::EligibilityCheckRequestAddress>>,
    /// The Employer Identification Number (EIN), distinct from the Federal Taxpayer Identification Number in `tin`. Only include when the payer requires it.
    #[serde(rename = "ein", skip_serializing_if = "Option::is_none")]
    pub ein: Option<String>,
    /// The provider's name, as either a person or an organization.
    #[serde(rename = "name")]
    pub name: Box<models::EligibilityCheckRequestProviderName>,
    /// The provider's [National Provider Identifier (NPI)](https://www.stedi.com/docs/healthcare/national-provider-identifier).  All healthcare providers eligible for an NPI must provide this identifier. Some non-traditional providers such as transportation services, durable medical equipment (DME) suppliers, or alternative medicine practitioners can't receive an NPI. If the provider doesn't have an NPI, payers virtually never support requests with alternate IDs. In the rare circumstance that a payer has instructed you to use an alternate ID, the payer will typically require you to supply either their `tin` or `ssn` instead.
    #[serde(rename = "npi", skip_serializing_if = "Option::is_none")]
    pub npi: Option<String>,
    /// The provider's pharmacy processor number. Only include when specifically instructed by a payer - for example, when the provider doesn't have an [NPI](https://www.stedi.com/docs/healthcare/national-provider-identifier). This use case is very rarely supported, and is typically when the provider is a non-medical provider, such as a social worker, home health aide, or transportation service.
    #[serde(rename = "pharmacyProcessorNumber", skip_serializing_if = "Option::is_none")]
    pub pharmacy_processor_number: Option<String>,
    /// Information about the provider's role and taxonomy. Only include when required by a payer.
    #[serde(rename = "provider", skip_serializing_if = "Option::is_none")]
    pub provider: Option<Box<models::EligibilityCheckRequestEntityProvider>>,
    /// The provider's service provider number. Only include when specifically instructed by a payer - for example, when the provider doesn't have an [NPI](https://www.stedi.com/docs/healthcare/national-provider-identifier). This use case is very rarely supported, and is typically when the provider is a non-medical provider, such as a social worker, home health aide, or transportation service.
    #[serde(rename = "serviceProviderNumber", skip_serializing_if = "Option::is_none")]
    pub service_provider_number: Option<String>,
    /// The provider's Social Security Number (SSN). - Only include when specifically instructed by a payer - for example, if the provider doesn't have an [NPI](https://www.stedi.com/docs/healthcare/national-provider-identifier). This use case is very rarely supported, and is typically when the provider is a non-medical provider, such as a social worker, home health aide, or transportation service. - If the payer has instructed you to send an EIN but the provider operates using their SSN, use `provider.tin` instead of this field. - Don't use this for Federally-administered programs, such as Medicare.
    #[serde(rename = "ssn", skip_serializing_if = "Option::is_none")]
    pub ssn: Option<String>,
    /// The provider's Federal Taxpayer Identification Number. This is typically the provider's EIN (Employer Identification Number), but you may use the provider's SSN if the provider doesn't have an EIN. Only include if the payer requires it.
    #[serde(rename = "tin", skip_serializing_if = "Option::is_none")]
    pub tin: Option<String>,
    /// The type of provider making the request.
    #[serde(rename = "type", skip_serializing_if = "Option::is_none")]
    pub r#type: Option<models::EligibilityCheckRequestProviderType>,
}

impl EligibilityCheckRequestProvider {
    /// Information about the entity requesting the eligibility check. This may be an individual practitioner, a medical group, a hospital, or another type of healthcare provider.  - You must provide the provider's name as either a person (with `name.person`) or an organization (with `name.organization`).  - You must also provide an identifier - this is typically the provider's [National Provider Identifier](https://www.stedi.com/docs/healthcare/national-provider-identifier) (`npi`). If the provider doesn't have an NPI, you can supply an alternative, such as their `tin` or `ssn`.  - Don't include additional properties, such as `tin` or `address`, unless they are specifically required or suggested by the payer.
    pub fn new(name: models::EligibilityCheckRequestProviderName) -> EligibilityCheckRequestProvider {
        EligibilityCheckRequestProvider {
            additional_information: None,
            address: None,
            ein: None,
            name: Box::new(name),
            npi: None,
            pharmacy_processor_number: None,
            provider: None,
            service_provider_number: None,
            ssn: None,
            tin: None,
            r#type: None,
        }
    }
}

